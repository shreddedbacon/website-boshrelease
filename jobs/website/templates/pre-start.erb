#!/bin/bash -ex
NGINX_DIR=<%= p('nginx_docroot') %>
apt-get install git -y
if [[ -d "$NGINX_DIR" ]]; then
  rm -rf $NGINX_DIR
fi
git clone <%= p('github_repo') %> $NGINX_DIR
chown -R vcap:vcap $NGINX_DIR


